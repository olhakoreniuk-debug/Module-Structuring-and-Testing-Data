let count = 0;

count = count + 1;

// Line 1 is a variable declaration, creating the count variable with an initial value of 0
// Describe what line 3 is doing, in particular focus on what = is doing
// Line 3 is updating the value of the count variable by adding 1 to its current value. 
// The = operator is an assignment operator that assigns the result of the expression on the right (count + 1) to the variable on the left (count).
//count + 1 is evaluated first: it takes the current value stored in count and adds 1 to it, producing a new number.
// The = is the assignment operator. It does not mean “equals” like in maths. It means “store the value on the right into the variable on the left.”
// So this line updates count by replacing its old value with the new value.
//Example: if count was 0, then count + 1 is 1, and the assignment stores 1 back into count.